// La lógica de IA se ha movido al backend (server/server.js).
// Este archivo se mantiene vacío o con utilidades mínimas para evitar errores de importación si fuera necesario en el futuro.
export const generateAdventureConcept = async () => {
  throw new Error("Esta función debe ser llamada a través del backend.");
};

export const getGeminiResponse = async () => {
  throw new Error("Esta función debe ser llamada a través del backend.");
};
